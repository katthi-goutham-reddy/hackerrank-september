# Developer Exploration & Diagnostic Scripts

These are internal debugging, data exploration, and diagnostic scripts used during development. They are not part of the core evaluation solution or production pipeline (which is fully encapsulated in `code/main.py`).
